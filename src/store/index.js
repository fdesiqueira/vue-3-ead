import { createStore } from 'vuex'

import users from "./modules/users"

export default createStore({
    state: {},
    getters: {},
    mutations: {},
    actions: {},
    modules: {
        users
    }
})